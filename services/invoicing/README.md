# Invoicing

Rust implementation of the Wave-A Invoicing bounded context. It owns invoice titles, deterministic SIM blue e-invoice requests, red-flush observations, and read-only itinerary receipt projections.
