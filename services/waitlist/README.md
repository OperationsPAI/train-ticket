# waitlist

Domain: Waitlist

Language: rust

Phase: future-scope

Status: placeholder-skeleton

## Owns

- WaitlistRequest
- QueuePolicy
- FulfillmentWindow

## DDD Sources

- `docs/02-domains/waitlist.md`

## Language Rationale

Rust fits fairness, mutual-exclusion, and queue-order invariants.

## Skeleton Check

```bash
cargo test
```

## Runtime Baseline

The service exposes the shared Rust Axum runtime baseline with `/health`, `/live`, `/livez`, `/ready`, `/readyz`, and `/metadata`. Request and correlation IDs are propagated or generated by the shared kernel runtime middleware; tracing remains opt-in through the no-op-by-default observer seam.
